# ⚡ Generator Functions Cheat Sheet (इंटरव्यू के लिए)

---

## 🔑 मुख्य बातें (Key Concepts)

| कॉन्सेप्ट (Concept) | क्या है (What is it) | सिंटैक्स (Syntax) |
| :--- | :--- | :--- |
| **Generator Function** | एक ख़ास फ़ंक्शन जिसे **रोका (pause)** और **फिर से शुरू (resume)** किया जा सकता है। | `function* myGenerator() { ... }` |
| **`yield`** | फ़ंक्शन को **रोकने** और एक **वैल्यू बाहर भेजने** के लिए इस्तेमाल होता है। फ़ंक्शन अपनी जगह याद रखता है। | `yield value;` |
| **Generator Object** | जब आप Generator फ़ंक्शन को कॉल करते हैं तो यह एक **Iterator** ऑब्जेक्ट लौटाता है। | `const iterator = myGenerator();` |
| **`.next()`** | Generator को **अगले `yield` तक चलाने** के लिए इस्तेमाल होता है। | `iterator.next()` |

---

## 🚦 Generator के चलने का तरीका (Execution Flow)

1.  **कॉल करना:** जब आप `myGenerator()` को कॉल करते हैं, तो फ़ंक्शन का कोड तुरंत नहीं चलता है। इसके बजाय, यह एक **Generator Object** लौटाता है।
2.  **पहला `.next()`:** कोड चलना शुरू होता है और पहले `yield` स्टेटमेंट तक पहुँचता है। यह **वैल्यू** बाहर भेजता है और **रुक जाता है**।
3.  **रिजल्ट फॉर्मेट:** `.next()` हमेशा यह ऑब्जेक्ट लौटाता है: `{ value: [yielded value], done: [true/false] }`
4.  **अगला `.next()`:** फ़ंक्शन उस जगह से **फिर से शुरू** होता है जहाँ वह रुका था, और अगले `yield` तक चलता है।
5.  **समापन:** जब फ़ंक्शन में कोई और `yield` नहीं बचता है, या जब यह `return` स्टेटमेंट तक पहुँचता है, तो `.next()` लौटाता है: `{ value: [returned value या undefined], done: true }`

---

## 🛠️ मुख्य `yield` उपयोग (Key `yield` Usage)

| उपयोग (Usage) | सिंटैक्स (Syntax) | व्याख्या (Explanation) |
| :--- | :--- | :--- |
| **वैल्यू भेजना** | `yield value;` | सबसे आम उपयोग। वैल्यू बाहर भेजकर रुक जाता है। |
| **वैल्यू प्राप्त करना** | `const input = yield;` | `.next(newValue)` के माध्यम से बाहर से **वैल्यू** Generator के अंदर लेना। |
| **Generator को सौंपना** | `yield* anotherGenerator();` | एक Generator को दूसरे Generator के सभी `yield` की ज़िम्मेदारी सौंपना (Delegating). |

---

## 🎯 उपयोग कब करें? (When to Use?)

1.  **Lazy Evaluation (आलसी मूल्यांकन):** जब आपको **अनंत (infinite) डेटा स्ट्रीम** या बहुत बड़े डेटा सीक्वेंस की ज़रूरत हो। Generator सिर्फ़ तभी वैल्यू जनरेट करते हैं जब उन्हें **`.next()` से मांगा** जाता है, जिससे मेमोरी बचती है।
    * *उदाहरण:* Fibonacci sequence या Unique IDs जनरेट करना।
2.  **Custom Iterators (कस्टम Iterator):** जब आप किसी Object के डेटा को `for...of` लूप के ज़रिए **अपने तरीके से** Iterate (घुमाना) करना चाहते हैं। Generator इसे बहुत आसान बना देते हैं।
3.  **State Management (स्थिति प्रबंधन):** **Coroutines** या State Machines बनाने के लिए, जहाँ फ़ंक्शन को बीच में रोककर उसकी स्थिति (state) को सहेजना ज़रूरी हो।

---

## 🤯 इंटरव्यू प्रश्न (Interview Questions)

* **Generator और Regular Function में क्या अंतर है?**
    * **Regular Function:** एक बार में पूरा चलता है और केवल एक वैल्यू (`return` से) लौटाता है।
    * **Generator:** रोका जा सकता है (`yield` से) और समय के साथ कई वैल्यू लौटाता है।
* **`yield` और `return` में क्या अंतर है?**
    * **`yield`:** फ़ंक्शन को **रोकता** है, एक वैल्यू देता है, और **फिर से शुरू** होने के लिए तैयार रहता है।
    * **`return`:** फ़ंक्शन को **हमेशा के लिए ख़त्म** कर देता है और फ़ाइनल वैल्यू लौटाता है (`done: true` सेट करता है)।
* **आप Generator में वैल्यू कैसे भेजते हैं?**
    * हम **`.next()`** मेथड में आर्गुमेंट पास करके (`iterator.next('My Value')`) Generator के अंदर वैल्यू भेज सकते हैं।