# 📝 Currying in JavaScript: आसान शब्दों में

Currying **Functional Programming** (फ़ंक्शनल प्रोग्रामिंग) की एक तकनीक है। यह एक ऐसा तरीका है जिससे हम एक फ़ंक्शन को बदल देते हैं जो एक साथ कई **arguments** (आर्गुमेंट्स/मान) लेता है।

---

## 💡 Currying क्या करता है?

Currying एक फ़ंक्शन को ऐसे फ़ंक्शंस की **श्रृंखला (chain)** में बदल देता है, जिसमें **हर फ़ंक्शन केवल एक argument** लेता है।

* **बदलाव:** `function(a, b, c)` $\rightarrow$ `function(a)(b)(c)`
* यह एक-के-बाद-एक, **आंशिक रूप से लागू (partially applied)** फ़ंक्शंस की एक चेन बनाता है।
* चेन में हर फ़ंक्शन **एक नया फ़ंक्शन** लौटाता है, जब तक कि **सभी arguments** नहीं मिल जाते।
* जब आखिरी argument मिल जाता है, तब **अंतिम परिणाम (final result)** की गणना (calculation) होती है।

---

## 🛠️ यह कैसे काम करता है (उदाहरण)?

मान लीजिए आपके पास एक `जोड़ (add)` फ़ंक्शन है जो तीन नंबर लेता है: `add(a, b, c)`.

Currying के बाद, यह ऐसा दिखेगा:

```javascript
// Curried Version
const add = (a) => (b) => (c) => a + b + c;

// उपयोग (Usage):
const addTwo = add(2); // यह एक नया फ़ंक्शन लौटाता है, जिसमें 'a' फिक्स हो गया है।
const addTwoAndThree = addTwo(3); // यह फिर से एक नया फ़ंक्शन लौटाता है।
const result = addTwoAndThree(5); // अब सभी arguments मिल गए, फाइनल रिजल्ट: 10

console.log(result); // Output: 10
// या सीधे:
console.log(add(2)(3)(5)); // Output: 10